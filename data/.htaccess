# Deze map bevat de database en de gekochte bestanden.
# Mag nooit via de browser bereikbaar zijn.
<IfModule mod_authz_core.c>
Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
Order deny,allow
Deny from all
</IfModule>
